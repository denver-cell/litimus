export default function StatusPage() {
  return (
    <div>
      <h1>litmus-backend</h1>
      <p>This is the Litimus API — not a page. See /api/detect, /api/usage, /api/daypass, /api/billing/payfast/*.</p>
    </div>
  );
}
